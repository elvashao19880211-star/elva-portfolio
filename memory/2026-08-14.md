# 2026-08-14 工作日志

## 支付闭环收尾（延续 08-13 未完成项）

### 完成的改动（commit `048e778`）
1. **会员自动激活**
   - `lib/userStore.ts`：User 接口加 `memberTier?`/`memberExpiresAt?`；新增 `activateMember(email, tier, days)`，续费累加（base = max(现有到期, now) + days）
   - `app/api/payment/notify/route.ts`：member 订单支付成功后调用 activateMember（365天），档位取 order.tier

2. **会员页显示状态**
   - `app/member/page.tsx`：当前身份卡片显示「会员有效至 YYYY-MM-DD」或「未开通会员」

3. **服务端已购查询 API**
   - `app/api/purchases/route.ts`（新）：GET 登录态（cookie token）→ 返回已购纹样列表
   - `app/api/payment/owned/route.ts`（新）：GET 按邮箱 → 返回已购纹样（详情页「已购」状态用）

4. **个人中心合并已购**
   - `app/account/page.tsx`：服务端已购 + localStorage 兜底，按 id+tier 去重合并

5. **详情页已购状态**
   - `components/PatternDetail.tsx`：owned 检查先查本地 hasPurchased，再查服务端 /api/payment/owned

### 发现并修复的问题
- `/api/payment/owned` 之前被 PatternDetail 引用但**从未创建**（运行时 404），本次补齐

### 补提交（之前漏的）
- `app/api/payment/create/route.ts`：src/patternType 参数
- `lib/orderStore.ts`：src/patternType 字段 + getPaidPatternsByEmail/listPaidPatternsByEmail
- `package-lock.json`：依赖清理
- `memory/2026-08-13.md`：昨日备忘

### 验证
- `/api/payment/owned?email=test@example.com` → 200 `{"patterns":[]}`
- `/api/purchases` → 200 `{"purchases":[]}`

### 后续方向
- 购买记录完全服务端化（userEmail 关联，跨设备）
- 会员到期自动失效处理（前端已判断 isMemberActive，服务端会员校验未做）
- 会员权益 gating（会员专属内容/下载）

## 素材库会员下载闭环（下午继续）

- ✅ `components/Lightbox.tsx`：加可选 `onDownload`/`downloadLabel` 下载按钮
- ✅ `app/materials/page.tsx`：
  - 会员状态检测（fetch `/api/auth/session` → `isMember`）
  - 下载权限拦截：会员直接下载 / 非会员弹开通会员
  - 会员弹窗：静态二维码 → 支付宝自动支付（复用 create API，`type: 'member'`）
  - 未登录买会员 → 跳 `/login?redirect=/materials`
- ✅ `backUrl` 支持：素材库买会员支付后跳回 `/materials`（orderStore Order 加 backUrl 字段，create 接收，return 优先用）
- 提交：`c319304`（下载闭环）、`5b34b11`（backUrl）
- ⚠️ 素材库目前还是示例图，下载的就是预览图本身；等真实素材到位后做「会员无水印原图 / 非会员带水印预览」区分


## 晚间收尾（2026-08-14 19:40）

- ✅ OSS 下载闭环生产验证通过：下载接口已上线，登录/参数校验生效，无水印下载成功
- ✅ 会员身份显示：会员页 + 个人中心加金色皇冠标识；个人中心 user 类型补上 memberExpiresAt/memberTier
- ✅ 收藏桃心位置调整：从图片右上角 → 信息区（纹样名称白底）右下角；样式改为红色描边空心 → 收藏实心
- 📌 待办：生产未配置 AUTH_SECRET（代码写死 fallback 'hetu-pattern-local-dev-secret-2026'，有伪造登录隐患，需补环境变量）
