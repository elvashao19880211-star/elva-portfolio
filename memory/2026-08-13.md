# 2026-08-13 进度备忘（续）

## 支付宝支付链路 — 完整闭环

### 已完成
- ✅ 纹样单件购买前端入口：`components/PatternDetail.tsx` 购买弹窗由静态收款码改为支付宝跳转支付
  - `handleAlipayPay`：调 `/api/payment/create`（type=pattern）→ `window.location.href = payUrl`
- ✅ 支付结果页 `app/api/payment/return/route.ts` 重构：
  - 区分会员/纹样订单类型，返回链接正确（会员→/member，纹样→/patterns/revival）
  - pending 时前端自动轮询（每 2s 查 status，最多 30 次），到账自动刷新
- ✅ 订单状态查询接口 `app/api/payment/status/route.ts`（GET，前端轮询用）
- ✅ **购买记录支付成功才入账**（关键修复）：
  - `lib/userData.ts` 新增 `savePendingPurchase`（暂存待支付）+ `commitPendingPurchase`（转正）
  - `handleAlipayPay` 跳转前 `savePendingPurchase`，不再直接 `addPurchase`
  - return 结果页 pollScript 轮询到 paid 后内联转正（写 `hetu_purchases`，去重）
  - PatternDetail 挂载时 `commitPendingPurchase()` 兜底（结果页未转正则补转正）

### 测试结果
- ✅ 用户真实支付 ¥19.9 成功（钱到账公司企业支付宝，即时到账，0.6% 手续费≈¥0.12）
- ✅ 收银台显示正确价格，证明 app 私钥签名正确
- ⚠️ 首次测试时购买记录逻辑未上线，个人中心空（已修复）

### 关键决策
- **购买记录"支付成功才入账"**：跳转前暂存 pending，结果页轮询到 paid 后转正，避免"点了支付没付钱也算已购"的白嫖漏洞
- **支付宝资金流向**：电脑网站支付 = 即时到账（FAST_INSTANT_TRADE_PAY），钱进企业支付宝余额，0.6% 手续费，可提现到对公账户

### 待办（下一阶段）
1. **购买记录迁移服务端**：localStorage 换浏览器/清缓存会丢，需迁到 orderStore + 用户关联（按 userEmail）
2. **会员权益自动激活**：notify 支付成功后，把会员到期时间写入 userStore（当前仅标记订单 paid）
3. **纹样详情"已购买"状态**：用户重新打开已购纹样详情，应直接显示"已购/下载"，而非"购买授权"
4. **纹样单件下载解锁**：个人中心已有"再次下载"，但详情弹窗缺 hasPurchased 初始化
5. **图片 DPI 处理**（MEMORY 提醒）：个人学习 72DPI+水印（sharp 缩放），商业 300DPI 原图
6. **支付宝商家版 App**：Elva 在另一台手机下载查账中

### 企业支付宝查账提示
- 登录需先退出个人支付宝（cookie 冲突）
- 电脑端 www.alipay.com → 企业账户/商家登录；手机端用"支付宝商家版"App
- 资金 → 余额/交易明细 可看到 ¥19.9 到账
